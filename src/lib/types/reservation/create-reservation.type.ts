export interface CreateReservationType {
    date: string;
    time: string;
    name: string;
    phone: string;
    quantity: number;
}

export interface CustomerDataType {
    name: string;
    phone: string;
    quantity: number;
}